#include </imageio/keys.txt>
#include </imageio/write_image.c>
#include </imageio/read_image.c>
#include </imageio/show_image.c>

// Check if image is null
if (image = nullptr && < 0) {
    break;
} else {
    continue;
}
